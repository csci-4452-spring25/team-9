name: Start Discord Bot

on: 
  workflow_dispatch: 

env: 
  TF_CLOUD_ORGANIZATION: "cmnguyen252"
  TF_API_TOKEN: "${{ secrets.TF_API_TOKEN }}"
  TF_WORKSPACE: "DiscordBot"
  CONFIG_DIRECTORY: "./DiscordBot"

jobs:
  create-env:
    runs-on: ubuntu-latest
    steps:
    - name: Create env file
      uses: SpicyPizza/create-envfile@v2.0
      with: 
        env_DISCORD_TOKEN: ${{ secrets.DISCORD_TOKEN }}
        env_GH_TOKEN: ${{ secrets.GH_TOKEN }}
        env_GH_REPO: ${{ secrets.GH_REPO }}
        env_GH_BRANCH: ${{ secrets.GH_BRANCH }}
        directory: ./
        file_name: .env
  terraform:
    if: github.repository != 'hashicorp-education/learn-terraform-github-actions'
    name: "Terraform Apply for Discord Bot"
    runs-on: ubuntu-latest
    permissions: 
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Upload Configuration
        uses: hashicorp/tfc-workflows-github/actions/upload-configuration@v1.0.0
        id: apply-upload
        with:
          workspace: ${{ env.TF_WORKSPACE }}
          directory: ${{ env.CONFIG_DIRECTORY }}

      - name: Create Apply Run
        uses: hashicorp/tfc-workflows-github/actions/create-run@v1.0.0
        id: apply-run
        with:
          workspace: ${{ env.TF_WORKSPACE }}
          configuration_version: ${{ steps.apply-upload.outputs.configuration_version_id }}

      - name: Apply
        uses: hashicorp/tfc-workflows-github/actions/apply-run@v1.0.0
        if: fromJSON(steps.apply-run.outputs.payload).data.attributes.actions.IsConfirmable
        id: apply
        with:
          run: ${{ steps.apply-run.outputs.run_id }}
          comment: "Apply Run from GitHub Actions CI ${{ github.sha }}"
